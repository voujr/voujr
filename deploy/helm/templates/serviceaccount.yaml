{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "voujr.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "voujr.labels" . | nindent 4 }}
{{- end }}
