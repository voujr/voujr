# golangci-lint configuration for voujr.
version: "2"

linters:
  default: standard
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - bodyclose
    - unconvert

formatters:
  enable:
    - gofmt

issues:
  max-same-issues: 0
